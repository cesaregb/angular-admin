'use strict';

angular.module('processAdminApp.constants')
  .factory('constants', function () {
    var factory = {};
    factory.API_ENDPOINT = "";
    factory.ORDER = {};
    return factory;
  });
