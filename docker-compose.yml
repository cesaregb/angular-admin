db:
  image: mongo
  ports:
    - "27017:27017"
  command: "--smallfiles --logpath=/dev/null"
web:
  build: .
  command: npm start
  volumes:
    - "./node_app:/myapp"
  ports:
    - "3001:3001"
  links:
    - "db:mongo"
