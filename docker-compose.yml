db:
  image: mongo
  ports:
    - "27017:27017"
  command: "--smallfiles --logpath=/dev/null"
web:
  image: process-admin:v1
  command: npm start
  volumes:
    - "./node_app:/myapp"
  ports:
    - "9000:9000"
  links:
    - "db:mongo"
